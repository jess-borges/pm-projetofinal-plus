<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufmg.dcc052.oncebook" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".book.ui.BookActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".character.ui.CharacterActivity"
            android:label="@string/title_activity_book" />
        <activity
            android:name=".relationship.ui.RelationshipActivity"
            android:label="@string/title_activity_relationship" />
    </application>

</manifest>
